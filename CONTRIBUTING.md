# Contributing to ctxshot

```bash
npm install
npm run build
npm run test:smoke
```

## 定位

轻量**会话简报**（目录树 + 脚本 + README 摘要 + git diff），不是 Repomix 那种全仓库打包。

## 发版

```bash
npm publish --access public
```
